#ifndef RANDOM_RECORD_GENERATOR_H
#define RANDOM_RECORD_GENERATOR_H

#include <string>
#include <vector>
using namespace std;

int generateRandomAGE(int lower, int upper);
int generateRandomID(int lower,int upper);
string generateRandomName(int syllables);
int generateSortedNums(int till);
#endif
